/**
 * 
 */

$(document).ready(function(){
// function for the floating button when cursor leave the button, must hide all options	
})

function openMenu(){
	$(".optionGroup .option").toggleClass("shown");
}
